{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [{
    "label": "local-web-server",
    "type": "shell",
    "command": "start-process ws -argumentlist '--log.format dev'",
    "problemMatcher": {"pattern":[{
        "regexp": "^(Listening on)",
        "message": 1
      }]
    }
  }]
}